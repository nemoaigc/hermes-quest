// Placeholder to confirm testing infrastructure is wired up correctly
import { describe, it, expect } from 'vitest'

describe('testing infrastructure', () => {
  it('vitest is configured', () => {
    expect(true).toBe(true)
  })
})
